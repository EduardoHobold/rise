<%= form_with(model: nota_trabalho, local: true) do |form| %>
  <% if nota_trabalho.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nota_trabalho.errors.count, "error") %> prohibited this nota_trabalho from being saved:</h2>

      <ul>
      <% nota_trabalho.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :aluno_id %>
    <%= form.select :aluno_id,
    Aluno.all.collect {|c| [c.pessoa.nome , c.id]} %>
  </div>

  <div class="field">
    <%= form.label :trabalho_id %>
    <%= form.select :trabalho_id,
    Trabalho.all.collect {|c| [c.disciplina.nome , c.id]} %>
  </div>

  <div class="field">
    <%= form.label :nota %>
    <%= form.text_field :nota %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
