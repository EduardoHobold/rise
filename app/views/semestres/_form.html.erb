<%= form_with(model: semestr, local: true) do |form| %>
  <% if semestr.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(semestr.errors.count, "error") %> prohibited this semestr from being saved:</h2>

      <ul>
      <% semestr.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
