<%= form_with(model: prova_livro, local: true) do |form| %>
  <% if prova_livro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(prova_livro.errors.count, "error") %> prohibited this prova_livro from being saved:</h2>

      <ul>
      <% prova_livro.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :livro_id %>
    <%= form.select :livro_id,
    Livro.all.collect {|c| [c.nome , c.id]} %>
  </div>

  <div class="field">
    <%= form.label :aluno_id %>
    <%= form.select :aluno_id,
    Aluno.all.collect {|c| [c.pessoa.nome , c.id]} %>
  </div>

  <div class="field">
    <%= form.label :nota %>
    <%= form.text_field :nota %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
